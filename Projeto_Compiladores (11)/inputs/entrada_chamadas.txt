inteiro soma(inteiro x, inteiro y) {
    inteiro resultado = x + y;
    retorna resultado;
}

inteiro multiplica(inteiro a, inteiro b) {
    inteiro res = a * b;
    retorna res;
}

inteiro principal() {
    inteiro a = 10;
    inteiro b = 5;
    inteiro c = 0;
    

    inteiro resultado1 = soma(a,b)
    inteiro resultado2 = multiplica(a, 2);
    exibir(resultado1);
    exibir(resultado2)
    
    retorna c;
}
